import 'package:flutter/material.dart';

class ReceiptPage extends StatelessWidget {
  final String? title;
  const ReceiptPage({super.key, this.title});
  @override
  Widget build(BuildContext context) {
    return const Text('Pagina de Ejemplo (recibos.dart)');
  }
}
