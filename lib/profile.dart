import 'package:flutter/material.dart';

class ProfilePage extends StatelessWidget {
  final String? title;
  const ProfilePage({super.key, this.title});
  @override
  Widget build(BuildContext context) {
    return const Text('Pagina de Perfil');
  }
}
